detection:
  scrfd:
    model: detection/scrfd/det_10g.onnx
    input:
      width: 640
      height: 640

embedding:
  provider: arcface
  model: embedding/arcface/w600k_r50.onnx
  input_size: 112

search:
  provider: faiss
  index_path: indexes/gallery.index
  metric: cosine
