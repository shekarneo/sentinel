profiles:
  enrollment:
    stages:
      - scrfd
      - alignment
      - assessment
      - fraud
      - embedding
    search:
      enabled: false

  attendance:
    stages:
      - scrfd
      - alignment
      - fraud
      - embedding
      - search
    search:
      enabled: true
      top_k: 5

  surveillance:
    stages:
      - scrfd
      - alignment
      - embedding
      - search
    search:
      enabled: true
      top_k: 1

  kyc:
    stages:
      - scrfd
      - alignment
      - assessment
      - fraud
      - embedding
      - search
      - verification
    search:
      enabled: true
      top_k: 5

  access_control:
    stages:
      - scrfd
      - alignment
      - fraud
      - embedding
      - search
      - verification
    search:
      enabled: true
      top_k: 3

  search:
    stages:
      - scrfd
      - alignment
      - embedding
      - search
    search:
      enabled: true
      top_k: 1
